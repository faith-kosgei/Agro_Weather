{
  "name": "agro-weather",
  "version": "1.0.0",
  "private": true,
  "description": "Farm weather intelligence — API + Dashboard monorepo",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix api\" \"npm run dev --prefix dashboard\"",
    "build": "npm run build --prefix api && npm run build --prefix dashboard",
    "lint": "npm run lint --prefix api && npm run lint --prefix dashboard",
    "test": "npm run test --prefix api",
    "install:all": "npm install --prefix api && npm install --prefix dashboard"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
